<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vehicleassistance">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <permission
        android:name="com.example.vehicleassistance.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />
    <meta-data
        android:name="com.google.android.maps.v2.API_KEY"
        android:value="@string/google_maps_key" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddShopActivity"
            android:label="Add Shop"
            android:theme="@style/SignUpTheme">

        </activity>
        <activity
            android:name=".AddVehicleActivity"
            android:label="Add Vehicle"
            android:theme="@style/SignUpTheme" />
        <activity
            android:name=".HomeScreenActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/SignUpTheme.NoActionBar" /> <!-- &lt;!&ndash; -->
        <!-- The API key for Google Maps-based APIs is defined as a string resource. -->
        <!-- (See the file "res/values/google_maps_api.xml"). -->
        <!-- Note that the API key is linked to the encryption key used to sign the APK. -->
        <!-- You need a different API key for each encryption key, including the release key that is used to -->
        <!-- sign the APK for publishing.tance -->
        <!-- (move screen reader cursor to ancestry tooltip within this table cell to be notified of when it has finished loading) -->
        <!-- vehicleassistance-d3bb2 -->
        <!-- You can define the keys for the debug and release targets in src/debug/ and src/release/. -->
        <!-- &ndash;&gt; -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:label="Forgot Password"
            android:theme="@style/SignUpTheme" />
        <activity android:name=".GoogleSignInActivity" />
        <activity
            android:name=".EmailActivity"
            android:label="Email"
            android:theme="@style/SignUpTheme" />
        <activity
            android:name=".NameActivity"
            android:label="Name"
            android:theme="@style/SignUpTheme" />
        <activity
            android:name=".SignUpActivity"
            android:label="Sign Up"
            android:theme="@style/SignUpTheme" />
        <activity android:name=".EnlargeImageActivityForMotorcycles"></activity>
        <activity android:name=".EnlargeImageActivityForCars" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>